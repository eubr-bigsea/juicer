import operation


def addresser(task, response):

#    if (task['id'] == 'RANDOM_SPLIT'):
#        random_split(task, response)

#    elif (task['id'] == 'DISTINCT_UNION'):
#        distinct_union(task, response)
